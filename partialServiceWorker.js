console.log("Run Partial Service");
/**
    This code will be bundled with the file placed in build.
    This will then be imported into the service worker to add functionality.
*/
self.addEventListener('sync', function(event) {
  event.waitUntil(
  );


});